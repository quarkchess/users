meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{HOST}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{USERNAME}}",
    "password": "{{PASSWORD}}"
  }
}

vars:pre-request {
  USERNAME: macaron
  PASSWORD: thisdoesnotmatter
}
